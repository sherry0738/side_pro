import React from 'react';
import './User.css';

const user = props => {
    // const answer = props.answers.map((answer, i) => {
    //     return (
    //         <li key={i}>
    //             {answer.title}: {answer.description}
    //         </li>
    //     );
    // });
    return (
        <div>
            <p>Here is the user and user's profile page.</p>
        </div>
    );
};

export default user;